# ==============================================================================
# LeadHunter Pro — Production Environment Variables
# Copy this file to '.env' before booting via Docker Compose or PM2
# ==============================================================================

# Core Application Settings
NODE_ENV=production
PORT=3000

# Security & Authentication Secrets
# Generate random HMAC and Session secrets (e.g., node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# SESSION_SECRET=your_secure_random_string_here
# TRACKING_HMAC_SECRET=your_secure_random_string_here

# Logging Verbosity
# Supported levels: silent, error, warn, info, debug, trace
LOG_LEVEL=info
